import { Section, SectionText, SectionTitle } from '../../styles/GlobalComponents';
import { LeftSection } from '../../styles/Intro.styled';

const Intro = (props) => (
  <Section row id="experience">
      <LeftSection>
          <SectionTitle main center>
              Organizations
          </SectionTitle>
      </LeftSection>
  </Section>
);

export default Intro;